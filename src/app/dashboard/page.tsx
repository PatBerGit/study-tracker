import styles from './page.module.scss'

export default function Dashboard() {
  return (
    <main className={styles.container}>
        <h1 className={styles.header}>Todo today</h1>
    </main>
  )
}